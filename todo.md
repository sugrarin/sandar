# TODO — SANDAR

## Технический долг

- [x] **Сессии не сохранялись** — `saveSession` получал `lastSession` вместо `SessionData`, числовые поля уходили как `undefined`. Исправлен маппинг в `page.tsx`.
- [x] **`createOptions()` мог вернуть < 4 вариантов** — добавлен sequential fallback.
- [x] **`pendingSessions` без лимита** — добавлен cap 50 записей в `lib/session.ts`.
- [x] **ErrorBoundary** — создан `components/ErrorBoundary.tsx`, оборачивает всё приложение.
- [x] **Нет уведомления о сбое сохранения** — добавлен `.save-error` toast в `page.tsx`.
- [x] **Валидация email до OTP-запроса** — regex-проверка в `AuthModal.tsx`.
- [x] **Ключ `authModal.invalidEmail` отсутствовал в переводах** — добавлен в KZ и RU.
- [ ] **Таймер во время фонового режима** — `Date.now()` считает время когда вкладка скрыта. Исправить через `visibilitychange` + пауза.
- [ ] **`syncPendingSessions` не ограничивает неудачные** — при постоянных ошибках failed-сессии накапливаются. Добавить лимит попыток или TTL.
- [ ] **Нет тестов** — критична логика `generateTask()` и `createOptions()`. Минимум: unit-тесты на edge cases (ответ = 0, диапазон = 1).

## UX

- [ ] **Keyboard shortcuts** — клавиши 1–4 для выбора ответа, Enter/Space для продолжения. В GameScreen нет ни одного `keydown`-обработчика.
- [ ] **Звуковые эффекты** — правильный/неправильный ответ, завершение раунда. Вкл/выкл в настройках.
- [ ] **Haptic feedback** — `navigator.vibrate()` на мобильных при ответе.
- [ ] **Streak дней** — `streak_days` уже считается в БД, но нигде не показан пользователю на главном экране.

## Функционал

- [ ] **Настраиваемый размер раунда** — 5 / 10 / 20 вопросов вместо жёстких 12.
- [ ] **Speed mode** — таймер на каждый вопрос (10–30 сек), истекает = ошибка.
- [ ] **История сессий с детализацией** — список последних раундов с раскрытием ответов.
- [ ] **Деление с остатком** — новый режим `remainder`.
- [ ] **Дроби** — простые операции: 1/2 + 1/4, 3/4 − 1/2 (только легкий уровень).
- [ ] **Leaderboard** — топ пользователей по XP или точности (только для залогиненных, опт-ин).

## Геймификация

- [x] XP-система (Easy 1 XP, Medium 2, Hard 3, Brain 5; бонус +50 за каждую 5-ю идеальную сессию подряд)
- [x] Уровни (каждые 1000 XP)
- [x] Streak сессий
- [x] Streak дней (счётчик в БД)
- [x] 9 достижений с прогрессом и тостом
- [ ] Push-уведомления о серии («не прерви серию»)
- [ ] Telegram-бот — напоминания о тренировках
- [ ] Email-дайджест — еженедельная статистика

## Инфраструктура

- [ ] **Rate limiting** — нет ограничений на auth-эндпоинты на уровне Next.js (Supabase Auth имеет свой лимит, но API routes открыты).
- [ ] **RLS на `session_answers`** — EXISTS-подзапрос медленный на больших данных; добавить прямую колонку `user_id` с индексом.
- [ ] **Логирование ошибок** — нет Sentry или аналога. В проде ошибки видны только в Vercel logs.
- [ ] **Экспорт статистики** — CSV или JSON для продвинутых пользователей.
