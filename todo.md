# TODO / Ideas for Sandar

## Критические баги и безопасность

- [x] **Pending sessions растут без ограничения** — добавлен лимит 50 записей в `lib/session.ts` (`pendingSessions.slice(-MAX_PENDING)`).
- [x] **Генерация вариантов ответа** — добавлен sequential fallback в `createOptions()`: если после 20 попыток Set < 4, добавляются числа 1, 2, 3… до заполнения.
- [x] **Сломанные вызовы `saveSession`** — в `page.tsx` данные передавались с неверной структурой (lastSession вместо SessionData), из-за чего сессии не сохранялись. Исправлен маппинг + добавлена функция `doSaveSession`.

## Надёжность и UX

- [x] **Error boundary** — создан `components/ErrorBoundary.tsx`, обёрнут весь `page.tsx`.
- [x] **Нет уведомления о сбое сохранения** — добавлен toast `.save-error` в `page.tsx` + стили в `globals.css`.
- [x] **Валидация email на клиенте** — добавлена regex-проверка в `AuthModal.sendCode()` до отправки запроса.
- [ ] **Таймер при смене вкладки** — `Date.now()` для длительности ненадёжен при сворачивании браузера. Использовать `document.visibilitychange` + пауза.

## Архитектура и качество кода

- [ ] **Разбить `gameStore.ts`** (390+ строк) на два слоя:
  - `taskGenerator.ts` — чистые функции генерации задач
  - `gameStore.ts` — только игровое состояние и переходы
- [ ] **Выделить константы** — строки режимов (`'addition'`, `'subtraction'` …) и `ROUND_SIZE = 12` разбросаны по коду. Вынести в `lib/constants.ts`.
- [x] **Несоответствие типов** — `SessionData` теперь экспортируется из `lib/session.ts`, маппинг из `lastSession` → `SessionData` исправлен в `page.tsx`.
- [x] **Ошибочные return-типы** — `advanceRound` и `finishCurrentRound` в интерфейсе `GameState` теперь правильно типизированы вместо `void`.
- [ ] **CSS-модули или scoped стили** — сейчас все стили в глобальном `globals.css` через CSS-переменные. При росте компонентов это станет проблемой. Мигрировать на CSS Modules.

## Производительность

- [ ] **Синхронная генерация задач** — `generateTasks()` блокирует JS-тред (некритично сейчас, но при режиме Extreme на слабых устройствах заметно). Рассмотреть `setTimeout(..., 0)` или Web Worker.
- [ ] **Конфетти в `ResultScreen`** — canvas-анимация пересчитывается на каждый кадр; убедиться что `requestAnimationFrame` корректно отменяется при размонтировании.

## Новые фичи

- [ ] **Статистика в реальном времени на UI** — сейчас stats не обновляются пока не перезагрузить страницу. Добавить `invalidate` после сохранения сессии.
- [ ] **История сессий на экране результатов** — показывать последние N сессий прямо после игры.
- [ ] **Streak и ачивки** — таблица `achievements` в схеме уже есть, но ничего не реализовано. Реализовать хотя бы базовые (10 сессий подряд, первая идеальная и т.д.).
- [ ] **Настраиваемый размер раунда** — сейчас `ROUND_SIZE = 12` жёстко задан. Дать пользователю выбор: 5 / 10 / 20 вопросов.
- [ ] **Рейтинговая таблица (Leaderboard)** — показывать топ-10 пользователей по точности/серии (опционально, только для авторизованных).
- [ ] **Режим на время** — ответь на 20 вопросов как можно быстрее.
- [ ] **Прогрессивная сложность** — автоматически повышать сложность если точность > 90% 3 раза подряд.

## Девопс и инфраструктура

- [ ] **Rate limiting на auth** — эндпоинты авторизации ничем не ограничены на уровне Next.js. Добавить rate limit middleware (напр. `upstash/ratelimit`) или включить в Supabase Auth настройки.
- [ ] **RLS политика `session_answers`** — EXISTS-подзапрос при каждом SELECT медленный на больших данных. Добавить прямую колонку `user_id` в `session_answers` для быстрого индекса.
- [ ] **Логирование ошибок** — нет Sentry/аналога. При production-крэшах нет алертов.
- [ ] **Тесты** — нет ни одного теста. Минимум: unit-тесты на `generateTasks()` и `createOptions()`, т.к. там нетривиальная логика.
